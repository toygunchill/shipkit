pr:
  titlePattern: '^\[ABC-\d+\] ([unclosed'
  forbidden: ["TBD", "TODO", "<!--"]
  sections:
    - name: Summary
      required: true
branch:
  pattern: '^(feature|bugfix|livebug)/[a-z0-9]+/[0-9]+-[a-z0-9]+(-[a-z0-9]+)*$'
jira:
  baseUrl: https://jira.example.com
  keyPattern: 'ABC-\d+'
